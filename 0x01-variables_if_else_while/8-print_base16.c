#include <stdio.h>

/**
 *main - Entry point for program
 *
 *Return: as zero as long as errornot encountered
 */
int main(void)
{
	int l[] = "0123456789";
	char hexadec[] = "abcdef";

	for (l = 10; l < 16; l++)
	{
		putchar(l[]);
		{
		putchar(hexadec[]);
		}
	}
	putchar('\n');
	return (0);
}
